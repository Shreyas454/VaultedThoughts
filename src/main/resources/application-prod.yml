

spring:
  redis:
    host: localhost
    port: 6379
  data:
    mongodb:
      uri: mongodb+srv://shreyasnagarkar454:GVbfc5ESFShUpVVy@cluster0.k228s.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: journaldb
      auto-index-creation: true
  application:
    name: journalApp

  mail:
    host: smtp.gmail.com #server that handles email sending and recieving    here we are using gmail but we can use any service like yahoo,outlook etc(look up their host name)
    port: 587 #encrypted smtp (if it were unencrypted it would have been 25)
    username: shreyasnagarkar454@gmail.com
    password: nhqv pnxf hsmn cexs
    properties:
      mail:
        smtp:
          auth: true # it means that a username and password are required
          starttls: #starts tls(transport layer security)
            enable: true # the data transfered between the app and the server will be encrypted (tls)


server:
  port: 8081
  servlet:
    context-path: /prod

weather:
  api:
    key: a2d6c811b83300c7af57a7928410cad3




#logging:
#  level:
#    com:
#      edigestjournal:
#        journalApp: DEBUG
